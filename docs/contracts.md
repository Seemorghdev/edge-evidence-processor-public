# Contracts

The existing processor observations, classifications, proposals, receipts, mutation matrices,
canonical encoding, and fingerprint report schemas are retained under `processor.contracts` and
`processor.processing`. Architecture convergence changes import locations only; serialized field
names, enum values, deterministic ordering, hashes, and CLI exit semantics are unchanged.
